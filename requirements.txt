# No third-party dependencies: as of Step 2 of PLAN.md the scripts use only
# the Python standard library (Python 3.9+).

# obsidian2site.py generation is standard-library-only. Building its output
# requires external tools — Hugo, the Ledger theme, Go for the Bluge server, and
# Pagefind — as documented in STATIC_SITE.md. They are not Python runtime
# dependencies.
